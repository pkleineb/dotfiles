local defaults = {
    file_explorer_command = "NvimTreeOpen",

    tab_config = {
        tabstop = 4,
        shiftwidth = 4,
        softtabstop = 4,
        expandtab = true,
    },

    exclude_file_types = {
        "NvimTree",
    },
}

return defaults
